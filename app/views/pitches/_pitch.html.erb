<div class="col-md-4 pitch">
<% if pitch.image? %>
 <%= image_tag pitch.image.url, class: "image-pitch"%>
 <% end %>
  <div class="pitch_information">
  <i class="fa fa-users" aria-hidden="true"></i>
    <%= link_to pitch.name, pitch %><br>
    rating: &nbsp;&nbsp;&nbsp;<%= pitch.rating %></i>
  </div>
</div>
<%#<%= image_tag(user.avatar.url(:thumb)) %>

